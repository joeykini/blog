{
  "name": "elias-sun-blog",
  "version": "1.0.0",
  "description": "Elias Sun 的極簡部落格",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve output",
    "deploy": "node build.js && vercel --prod",
    "new": "node create-post.js"
  },
  "dependencies": {
    "marked": "^4.3.0",
    "gray-matter": "^4.0.3",
    "fs-extra": "^11.1.1",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  }
} 